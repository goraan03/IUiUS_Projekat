<Window x:Class="IUiUS_Projekat.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Kursevi" Height="500" Width="800"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Dugmad za akcije -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0 0 0 10">
            <Button x:Name="DodajButton" Content="Dodaj kurs" Width="100" Margin="0,0,10,0" Click="Dodaj_Click" />
            <Button x:Name="ObrisiButton" Content="Obriši označene" Width="120" Click="Obrisi_Click" />
        </StackPanel>

        <!-- Tabela sa kursevima -->
        <DataGrid x:Name="KurseviGrid" Grid.Row="1" AutoGenerateColumns="False" CanUserAddRows="False" Margin="0 10 0 10" IsReadOnly="True">
            <DataGrid.Columns>
                <!-- CheckBox kolona -->
                <DataGridTemplateColumn Width="30">
                    <DataGridTemplateColumn.Header>
                        <CheckBox x:Name="SelectAllBox" Checked="SelectAll_Checked" Unchecked="SelectAll_Unchecked"/>
                    </DataGridTemplateColumn.Header>
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding IsSelected}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <!-- Hyperlink za naziv -->
                <DataGridTemplateColumn Header="Naziv" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock>
                                <Hyperlink Click="Naziv_Click">
                                    <Run Text="{Binding Naziv}" />
                                </Hyperlink>
                            </TextBlock>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <!-- Slika -->
                <DataGridTemplateColumn Header="Slika" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Image Source="{Binding SlikaPath}" Width="80" Height="50" Stretch="UniformToFill"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <!-- Datum -->
                <DataGridTextColumn Header="Datum dodavanja" Binding="{Binding DatumDodavanja, StringFormat=\{0:dd.MM.yyyy HH:mm\}}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Dugme za izlaz (opciono) -->
        <Button Content="Odjavi se" Grid.Row="2" HorizontalAlignment="Right" Width="100" Click="Izlaz_Click" />
    </Grid>
</Window>
