<Window x:Class="IUiUS_Projekat.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Prijava"
        Height="400" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{StaticResource BackgroundBrush}"
        WindowStyle="None">

    <Border CornerRadius="10" Padding="30" Background="{StaticResource PanelBrush}">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="Prijava na sistem" FontSize="20" FontWeight="Bold" Margin="0,0,0,20" Foreground="White"/>

            <TextBlock Text="Korisničko ime:" Style="{StaticResource LabelStyle}"/>
            <TextBox x:Name="UsernameBox" Width="250"/>

            <TextBlock Text="Lozinka:" Style="{StaticResource LabelStyle}" Margin="0,10,0,0"/>
            <PasswordBox x:Name="PasswordBox" Width="250"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                <Button Content="Prijavi se" Width="100" Click="Login_Click"/>
                <Button Content="Izlaz" Width="100" Click="Exit_Click"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>